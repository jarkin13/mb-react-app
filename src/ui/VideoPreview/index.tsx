import { VideoDescriptionText, VideoPreview, VideoTitleText } from "./subcomponents";

interface VideoPreviewProps {
  text: string;
}
const VideoPreview = ({VideoDescriptionText, VideoPreview, VideoTitleText}) => {
  return(

  )
}